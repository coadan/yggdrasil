apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "panels.fullname" . }}
spec:
  template:
    spec:
      containers:
        - name: web
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
