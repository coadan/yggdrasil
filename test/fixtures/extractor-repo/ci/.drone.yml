kind: pipeline
type: docker
name: default

trigger:
  event:
    - push

steps:
  - name: test
    image: clojure:tools-deps
    environment:
      TEST_DB: panels
    commands:
      - bb test
  - name: deploy
    image: alpine:3.20
    depends_on: [test]
    commands:
      - bb deploy
