version: 2.1

orbs:
  node: circleci/node@5.1.0

executors:
  clojure:
    docker:
      - image: cimg/clojure:1.11

jobs:
  test:
    executor: clojure
    steps:
      - checkout
      - restore_cache:
          keys:
            - deps-v1
      - run: bb test
      - store_artifacts:
          path: target/report
  deploy:
    executor: clojure
    steps:
      - run: bb deploy

workflows:
  build:
    jobs:
      - test
      - deploy:
          requires:
            - test
