openapi: 3.1.0
info:
  title: Panels API
  version: 1.0.0
paths:
  /panels:
    get:
      operationId: listPanels
      responses:
        "200":
          description: ok
    post:
      operationId: createPanel
      responses:
        "201":
          description: created
  /panels/{id}:
    get:
      operationId: getPanel
      responses:
        "200":
          description: ok
components:
  schemas:
    Panel:
      type: object
    PanelCreate:
      type: object
